const config = require("@ui5/webcomponents-tools/components-package/wdio.js");

// For test debugging purposes - removed the headless arg and sets larger timeout
// config.config.capabilities[0]["goog:chromeOptions"].args = ['--disable-gpu'];
// config.config.mochaOpts.timeout = (24 * 60 * 60 * 1000);

module.exports = config;
