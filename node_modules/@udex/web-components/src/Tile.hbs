{{#if interactive}}
    <a
        class="udex-tile"
        href="{{href}}"
        target="{{target}}"
        aria-label="{{accessibleName}}"
        @keydown={{_onkeydown}}
    >
        <slot></slot>
    </a>
{{else}}
    <div class="udex-tile">
        <slot></slot>
    </div>
{{/if}}
